
  # Gold Link Financial

  This is a code bundle for Gold Link Financial. The original project is available at https://www.figma.com/design/2JSGA40p64AEj1f1kCtpxx/Gold-Link-Financial.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  