# Dependencies & build output
node_modules
**/node_modules
dist
apps/*/dist
packages/*/dist

# Lockfiles & generated artifacts
pnpm-lock.yaml

# JSON data (treated as fixtures, not source code)
db/*.json

# Long-form docs — keep author's formatting
*.md

# Parallel worker scope (apps/mobile is owned by another worker, may not exist yet)
apps/mobile

# TODO: enable Prettier on apps/web once dust settles.
# A parallel worker is concurrently editing apps/web/src/app/components/
# (JSON-wiring), and a number of pre-existing files (main.tsx, vite.config.ts,
# index.html, postcss.config.mjs, styles/theme.css, ui/*.tsx) have not yet
# been run through Prettier. Narrow this ignore once the JSON-wiring worker
# has landed and the web app has been formatted in a follow-up pass.
apps/web
